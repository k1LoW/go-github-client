# generated by octocov init
coverage:
  if: true
  paths:
    - v33/coverage.out
codeToTestRatio:
  code:
    - 'v33/**/*.go'
    - '!v33/**/*_test.go'
  test:
    - 'v33/**/*_test.go'
testExecutionTime:
  if: true
diff:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
comment:
  if: is_pull_request
summary:
  if: true
report:
  if: is_default_branch
  datastores:
    - artifact://${GITHUB_REPOSITORY}
